{
  "name": "instagram-playwright-automation",
  "version": "1.0.0",
  "description": "This is a UI automation assignment project developed using Playwright and TypeScript.",
  "main": "",
  "scripts": {
    "test": "npx playwright test --headed && npx playwright show-report",
    "headless-test": "npx playwright test && npx playwright show-report",
    "debug-test-dev": "npx playwright test --ui"
  },
  "keywords": [
    "playwright",
    "typescript",
    "nodejs",
    "web-automation",
    "test-automation"
  ],
  "author": "Mahendiran M",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@types/node": "^20.11.30"
  },
  "dependencies": {
    "@types/chance": "^1.1.6",
    "chance": "^1.1.12",
    "crypto-js": "^4.2.0",
    "playwright": "^1.50.1"
  }
}
